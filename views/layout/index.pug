include ../_config.pug

doctype html
html

  head
    meta(charset="utf-8")
    meta(name='viewport', content='width=device-width,user-scalable=no,initial-scale=1.0')
    title #{self.title}

    script.
      let userAgent = window.navigator.userAgent.toLowerCase();
      if(userAgent.indexOf('msie') !== -1 || userAgent.indexOf('trident') !== -1 ) location.href="unsupported.html";
      
    noscript.
      
      JavaScriptが有効になっていないため、正常に動作することができません。

    script(src="/bundle.js")
    link(rel="stylesheet", href="http://code.jquery.com/ui/1.10.3/themes/cupertino/jquery-ui.min.css")
    <script>FontAwesomeConfig = { searchPseudoElements: true };</script>
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.0.13/css/all.css" integrity="sha384-DNOHZ68U8hZfKXOrtjWvjxusGo9WQnrNx2sqG0tfsghAvtVlRW3tvkXWZh58N9jp" crossorigin="anonymous">

  body(data-cntId=self.contentId, data-usertype=user_type)
    .wrap-body-content
      .modal-cntnr
        .overlay-block
        .modal-cnt#modal-close
          .modal-box.shadow-d
            #helpModal(data-modal="hide")
              include ../modal/help.pug
            #noticeModal(data-modal="hide")
              include ../modal/notice.pug
            #imageModal(data-modal="hide")
              include ../modal/imageModal.pug
            block modal
            .modal-close.shadow-d#modal-close-btn
              i.icon-close.fas.fa-times


      block float_content
      .wrapper
        block header
        .main-cntnr(id=`${self.contentId}Section`)
          block content
      if self.contentId != "register"
        include ./footer.pug
        

    #progress.progress-cntnr
      .cnt-progress.flex._jc
        .loading
          i.icon-progress.fas.fa-circle-notch.fa-spin
