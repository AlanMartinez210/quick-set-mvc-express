-
  var user_id = self.userId;
  var user_type = self.userType;
  var header_data = self.headerData;
  var body_data = self.bodyData;
  
  console.log("*******************************************************************");
  console.log(`######## user id : ${user_id} | user type : ${user_type} ##########`);
  console.log(`content id -> : ${self.contentId}`);
  console.log("header data -> :");
  console.log(header_data);
  console.log("body data -> :");
  console.log(body_data);
  console.log("*******************************************************************");
-

doctype html
html

  head
    meta(charset="utf-8")
    meta(name='viewport', content='width=device-width,user-scalable=no,initial-scale=1.0')
    title #{self.title}
    script(src="/bundle.js")
    link(rel="stylesheet", href="http://code.jquery.com/ui/1.10.3/themes/cupertino/jquery-ui.min.css")
    <script>FontAwesomeConfig = { searchPseudoElements: true };</script>
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.0.13/css/all.css" integrity="sha384-DNOHZ68U8hZfKXOrtjWvjxusGo9WQnrNx2sqG0tfsghAvtVlRW3tvkXWZh58N9jp" crossorigin="anonymous">
  
  body(data-cntId=self.contentId)
    .wrap-body-content
      .modal-cntnr
        .overlay-block
        .modal-cnt#modal-close
          .modal-box.shadow-d
            block modal
            .modal-close.shadow-d#modal-close-btn
              i.icon-close.fas.fa-times

      block float_content
      .wrapper
        block header
        .main-cntnr(id=`${self.contentId}Section`)
          block content
      footer
        .ftr-cntnr
          .ftr-cnt
            p &copy; 2018 TYPEs - c2Link

    #progress.progress-cntnr
      .cnt-progress.flex._jc
        .loading
          i.icon-progress.fas.fa-circle-notch.fa-spin
