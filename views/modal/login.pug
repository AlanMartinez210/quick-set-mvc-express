#loginModal.login-ptm(data-modal="hide")
  form(method="post", action="/login", name="loginForm")

    .board-prm

      .board-cnt._s

        .label-input-box#box_login_key
          .label-box.icon_tip._edit.help-tip(data-help_text=viewMsg.v00004)
            p.main-label: |ユーザーID、またはメールアドレス
          .input-box.flex._c
            input(type="text", name="login_key", maxlength=256)
          p.bottom-label #{self.err_message}

        .label-input-box#box_login_password
          .label-box.icon_tip._edit.help-tip(data-help_text=viewMsg.v00001)
            p.main-label: |パスワード
          .input-box.flex._c
            input(type="password", name="login_password", autocomplete="password", maxlength=32)
          p.bottom-label #{self.err_message}

      .board-ftr._s.flex._c._jc
        button(type="submit", name="doLogin").btn-prm.st-back-b.shadow-l
          |ログイン
