#editBgCoverImgModal.login-ptm(data-modal="hide")
  form(method="post", action="", name="editBgCoverImgForm")

    .board-prm
      .board-hdr
        .label-box._fb.flex._c
          i.icon-title-b.far.fa-bell.fa-fw.c-glay
          span: |画像の変更
      .board-cnt._s
        .attention-prm
          p
            |200kまでの画像(jpg/gif/png)が設定できます。
        
        .label-input-box
          .custom-box._s
            label(for="up_cover_image").btn-prm.st-line-b.shadow-l
              |画像を選択
            input.init-hidden(type="file", id="up_cover_image", name="up_cover_image", accept="image/*")

        .label-input-box(data-mdc="trimming_box")
          .label-box
            p.main-label: |トリミング
          .custom-box._s
            .option-box.flex._jr
              span.option-btn.st-line-b(name="cropper_reset")
                |トリミングをリセット
            .image_wrap.st-mt-s
              img(id="coverThumbnail", src="", class="cropper-img")

        .label-input-box(data-mdc="preview_box")
          .label-box
            p.main-label: |プレビュー
          .custom-box._s
            .cover-box-wrap
              .cover-box.cover_crop_preview.shadow-d
      
      .board-wrap(data-mdc="board-ftr")
        .board-ftr._s.flex._c._jc
          button(type="submit", name="doPostCoverData").btn-prm.st-back-b.shadow-l
            |登録