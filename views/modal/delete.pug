#deleteModal.login-ptm(data-modal="hide")
  form(method="post", action="/delete", name="userDeleteForm")

    .board-prm

      .board-hdr
        .label-box._fb.flex._c
          i.icon-title-b.fas.fa-minus-circle.fa-fw.c-glay
          span: |アカウント削除

      .board-cnt

        .attention-prm
          p
            span
              |※アカウントの削除について<br /><br />
              |・削除してから<span class="c-red">7日以内</span>を復帰期間とし、再ログインした場合はアカウントの復元が行われます。<br /><br />
              |・復帰期間を過ぎてのアカウントの復元はできません。<br /><br />
              |・アカウントを削除すると、<span class="c-red">すべてのデータ</span>が削除されます。<br />
        
        .label-input-box#box_consent_delete
          .label-box
            p.main-label: |<span class="c-red">※必ず上記をお読みください。</span>
          .check-box.flex._c
            input(id="consentDelete" type="checkbox" name="consent_delete")
            label(for="consentDelete")
              |確認の上、同意します。
          p.bottom-label

        .label-input-box#box_password
          .label-box
            p.main-label: |ログイン時のパスワードを入力してください。
          .input-box.flex._c
            input(type="password", name="password", autocomplete="password")
          p.bottom-label #{self.err_message}
          
      .board-ftr._s.flex._c._jc
        button(type="submit", name="doUserDelete").btn-prm.st-back-r.shadow-l
          |削除する
