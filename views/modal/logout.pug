#logoutModal.login-ptm(data-modal="hide")
  form(method="post", action="/logout", name="logoutForm")

    .board-prm

      .board-hdr
        .label-box._fb.flex._c
          i.icon-title-b.fas.fa-sign-out-alt.fa-fw.c-blue
          span: |ログアウト

      .board-cnt
        |ログアウトします。よろしいですか？

      .board-ftr._s.flex._c._jc
        button(type="submit", name="doLogout").btn-prm.st-back-b.shadow-l
          |ログアウト
        
