header
  .hdr-cntnr
    .cntnr-balancer
      .header-spacer

      //- - if (ShortcutMenu){
      //- .hdr-controll
      //-   .hdr-select-box._s.flex._c.shadow-l
          
      //-     label.selectBox.flex._c
      //-       select#scMenu(data-scMenuSelected="")
              
      //-         - for (let i=0;i<sc_menu_list.length;i++){ const m = sc_menu_list[i];
      //-           - if (ml[i].type == session.user.type){
      //-             option(value=m.id, data-href=m.id) #{m.name}
      //-           - }
      //-         - }

      //-       i.fas.fa-caret-down.icon-title
      //- - }

      - if (backBtn.href){
      .hdr-controll
        a.btn-prm.link-btn.st-btn-icon.st-line-w.flex._c(href=backBtn.href)
          i.icon-btn.fas.fa-chevron-left.fa-fw
          span: |#{backBtn.name}
      - }

    .hdr-menu
      .cntnr-balancer
        //-title
        .hdr-title.flex._c
          .hdr-logo
            img(src=main_logo, alt="", srcset="")
          h1: |#{self.title}
          span#noticeBtn.act-back-blue.shadow-d.on-notice
            i.icon_notice.far.fa-bell
        //-navigation
        nav
          ul.hdr-g-nav.flex._c
            each menu in gnav_menu_list
              - if(menu.id == "cosgroup" && self.userType == "2") continue;
              li(id=menu.id)
                a.flex._c._j(href=menu.href)
                  span.menu-icon-poti-lock
                    i.icon-menu.fa-fw(class=menu.icon_css)

